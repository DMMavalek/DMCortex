<Window x:Class="DungeonMasterCortex.Views.ActivationRequestDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Request Activation"
        Width="540"
        Height="420"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#1e1e1e"
        Foreground="#d4af37"
        ResizeMode="NoResize">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Activation Request"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1"
                   Text="Enter your contact information. The app will generate an activation request code and send it by email."
                   TextWrapping="Wrap"
                   Foreground="#e0e0e0"
                   Margin="0,0,0,12"/>

        <StackPanel Grid.Row="2" Margin="0,0,0,8">
            <TextBlock Text="Name" Foreground="#e0e0e0" Margin="0,0,0,4"/>
            <TextBox x:Name="TxtName" MinWidth="420"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,8">
            <TextBlock Text="Email" Foreground="#e0e0e0" Margin="0,0,0,4"/>
            <TextBox x:Name="TxtEmail" MinWidth="420"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Margin="0,0,0,8">
            <TextBlock Text="Phone" Foreground="#e0e0e0" Margin="0,0,0,4"/>
            <TextBox x:Name="TxtPhone" MinWidth="420"/>
        </StackPanel>

        <Border Grid.Row="5" BorderBrush="#3d3d3d" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,8,0,0">
            <TextBlock x:Name="TxtStatus"
                       Text="Status: Waiting for details"
                       Foreground="#c0c0c0"
                       TextWrapping="Wrap"/>
        </Border>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Send Request"
                    Style="{StaticResource GoldButton}"
                    Width="140"
                    Margin="0,0,10,0"
                    Click="BtnSendRequest_Click"/>
            <Button Content="Close"
                    Style="{StaticResource GoldButton}"
                    Width="100"
                    Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
